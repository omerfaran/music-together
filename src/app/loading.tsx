import { LoadingComponent } from "@/components/LoadingComponent";

// file must be named 'loading', and here component can have a capital L
const Loading = () => {
  return <LoadingComponent />;
};

export default Loading;
